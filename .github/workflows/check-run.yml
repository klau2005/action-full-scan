name: Check run

on:
  push:
    branches:
      - master
    paths-ignore:
      - '**.md'
  pull_request:
    paths-ignore:
      - '**.md'
  workflow_dispatch:

jobs:
  check-run:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: ZAP Scan
      uses: ./
      id: action-full-scan
      with:
        target: 'https://google-gruyere.appspot.com/643381732845760668651772690991859721406/'
        allow_issue_writing: false
